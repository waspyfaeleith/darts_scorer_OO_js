# darts_scorer_OO_js
OO Darts Scorer written in JavaScript and with tests
Runs using node

To load packages:

```
$ npm install
```
To run the app:

```
$ node app.js
```

To run the tests:
```
$ npm test
```

